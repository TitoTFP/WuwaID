name: Build minimal version.dll proxy

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Create minimal proxy project
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path minimal_proxy | Out-Null

          Copy-Item pakbypass/VersionProxy.hpp minimal_proxy/VersionProxy.hpp
          Copy-Item pakbypass/Logger.hpp minimal_proxy/Logger.hpp
          Copy-Item pakbypass/version.def minimal_proxy/version.def

          @'
          #include <windows.h>
          #include <string>
          #include "VersionProxy.hpp"

          BOOL APIENTRY DllMain(HMODULE hModule, DWORD ul_reason_for_call, LPVOID lpReserved)
          {
              switch (ul_reason_for_call)
              {
              case DLL_PROCESS_ATTACH:
                  DisableThreadLibraryCalls(hModule);
                  VersionProxy::LoadRealDll();
                  break;

              case DLL_PROCESS_DETACH:
                  VersionProxy::Unload();
                  break;
              }

              return TRUE;
          }
          '@ | Set-Content minimal_proxy/dllmain.cpp -Encoding UTF8

          @'
          <?xml version="1.0" encoding="utf-8"?>
          <Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
            <ItemGroup Label="ProjectConfigurations">
              <ProjectConfiguration Include="Release|x64">
                <Configuration>Release</Configuration>
                <Platform>x64</Platform>
              </ProjectConfiguration>
            </ItemGroup>

            <PropertyGroup Label="Globals">
              <VCProjectVersion>17.0</VCProjectVersion>
              <Keyword>Win32Proj</Keyword>
              <ProjectGuid>{11111111-1111-1111-1111-111111111111}</ProjectGuid>
              <RootNamespace>MinimalVersionProxy</RootNamespace>
              <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>
            </PropertyGroup>

            <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />

            <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'" Label="Configuration">
              <ConfigurationType>DynamicLibrary</ConfigurationType>
              <UseDebugLibraries>false</UseDebugLibraries>
              <PlatformToolset>v143</PlatformToolset>
              <WholeProgramOptimization>true</WholeProgramOptimization>
              <CharacterSet>Unicode</CharacterSet>
            </PropertyGroup>

            <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />

            <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'">
              <TargetName>version</TargetName>
              <OutDir>$(ProjectDir)x64\Release\</OutDir>
              <IntDir>$(ProjectDir)x64\Release\obj\</IntDir>
            </PropertyGroup>

            <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'">
              <ClCompile>
                <WarningLevel>Level3</WarningLevel>
                <Optimization>MaxSpeed</Optimization>
                <FunctionLevelLinking>true</FunctionLevelLinking>
                <IntrinsicFunctions>true</IntrinsicFunctions>
                <SDLCheck>false</SDLCheck>
                <PreprocessorDefinitions>NDEBUG;_WINDOWS;_USRDLL;_WINDLL;UNICODE;_UNICODE;%(PreprocessorDefinitions)</PreprocessorDefinitions>
                <LanguageStandard>stdcpp20</LanguageStandard>
                <RuntimeLibrary>MultiThreaded</RuntimeLibrary>
                <AdditionalIncludeDirectories>.;%(AdditionalIncludeDirectories)</AdditionalIncludeDirectories>
              </ClCompile>
              <Link>
                <SubSystem>Windows</SubSystem>
                <EnableCOMDATFolding>true</EnableCOMDATFolding>
                <OptimizeReferences>true</OptimizeReferences>
                <GenerateDebugInformation>true</GenerateDebugInformation>
                <ModuleDefinitionFile>version.def</ModuleDefinitionFile>
              </Link>
            </ItemDefinitionGroup>

            <ItemGroup>
              <ClCompile Include="dllmain.cpp" />
            </ItemGroup>

            <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
          </Project>
          '@ | Set-Content minimal_proxy/minimal_proxy.vcxproj -Encoding UTF8

      - name: Build minimal version.dll proxy
        shell: cmd
        run: |
          msbuild minimal_proxy\minimal_proxy.vcxproj ^
            /p:Configuration=Release ^
            /p:Platform=x64 ^
            /m ^
            /v:normal

      - name: Show outputs and dependencies
        shell: pwsh
        run: |
          Get-ChildItem -Recurse minimal_proxy -Include *.dll,*.pdb,*.lib,*.exp | Select-Object FullName

      - name: Upload minimal version.dll
        uses: actions/upload-artifact@v4
        with:
          name: minimal-version-proxy
          path: |
            minimal_proxy/x64/Release/version.dll
            minimal_proxy/x64/Release/version.pdb